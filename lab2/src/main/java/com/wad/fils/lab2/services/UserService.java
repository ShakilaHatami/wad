package com.wad.fils.lab2.services;

import java.sql.*;

public class UserService {
  public boolean userExists(final String username){
    /* TODO 4
Usernames should be unique. Create a database with the table users with two columns, username and password. Populate it with some usernames. Create another class, UserService, which exposes a method

public boolean userExists(String username)
which returns true if the username exists in the table and false if not.

Call the method from the servlet so that the response will contain the line USER ALREADY EXISTS if the method returns true for the parameter username.
     */

    return  false;
  }
}
